// js/pages/landing.js
// Landing page initializer — mostly static HTML; minimal JS needed.
// The live sim clock is injected into the topbar by initNavUI in ui.js.

export function initLandingPage(_data, _user) {
  // Nothing to initialise — all content is static HTML in index.html.
}
