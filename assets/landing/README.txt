Place the four landing page hero images here with the following filenames:
  hero-1.jpg  — Main hero background (Parliament / Westminster imagery)
  hero-2.jpg  — "Simulation is Live" section break
  hero-3.jpg  — "How it Works" section break
  hero-4.jpg  — Footer CTA background

These images are referenced in index.html (landing page) and degrade gracefully
if missing (the sections still display with the navy gradient background).
